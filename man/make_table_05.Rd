% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_05.R
\name{make_table_05}
\alias{make_table_05}
\title{FDA Table 5: Duration of Treatment Exposure, Safety Population, Pooled Analyses}
\usage{
make_table_05(
  df,
  denominator = NULL,
  return_ard = TRUE,
  id_var = "USUBJID",
  arm_var = "TRT01A",
  saffl_var = "SAFFL",
  trtsdtm_var = "TRTSDTM",
  trtedtm_var = "TRTEDTM",
  u_trtdur = "days"
)
}
\arguments{
\item{df}{(\code{data.frame})\cr dataset (typically ADSL) required to build the table.}

\item{denominator}{(\code{data.frame} or \code{NULL})\cr dataset used as the denominator. If \code{NULL},
\code{df} is used.}

\item{return_ard}{(\code{flag})\cr whether an ARD should be returned. Defaults to \code{TRUE}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable used to split table into columns.
Defaults to \code{"TRT01A"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{trtsdtm_var}{(\code{character})\cr name of the treatment start datetime variable. Defaults to \code{"TRTSDTM"}.}

\item{trtedtm_var}{(\code{character})\cr name of the treatment end datetime variable. Defaults to \code{"TRTEDTM"}.}

\item{u_trtdur}{(\code{character})\cr unit for treatment duration. One of \code{"days"}, \code{"weeks"},
\code{"months"}, or \code{"years"}. Defaults to \code{"days"}.}
}
\value{
A \code{gtsummary} table, or if \code{return_ard = TRUE}, a named list with elements \code{table} and \code{ard}.
}
\description{
Creates FDA Table 5 showing duration of treatment exposure, including summary statistics and
patient counts by duration category.
}
\details{
\itemize{
\item \code{df} must contain the variables specified by \code{arm_var}, \code{saffl_var}, \code{id_var}, \code{trtsdtm_var},
and \code{trtedtm_var}.
\item Flag variables (i.e. \code{XXXFL}) are expected to have two levels: \code{"Y"} (true) and \code{"N"} (false).
Missing values in flag variables are treated as \code{"N"}.
\item Records with missing treatment start and/or end datetime are excluded from all calculations.
\item Duration categories are based on months (regardless of \code{u_trtdur}).
\item When \code{return_ard = TRUE}, returns a named list with elements \code{table} and \code{ard}.
}
}
\examples{
\dontshow{if (requireNamespace("pharmaverseadam", quietly = TRUE) && requireNamespace("dplyr", quietly = TRUE) && requireNamespace("lubridate", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

adsl <- pharmaverseadam::adsl |>
  filter(TRT01A != "Screen Failure")

result <- make_table_05(df = adsl)
result$table
\dontshow{\}) # examplesIf}
}
