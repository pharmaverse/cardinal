% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_04.R
\name{make_table_04}
\alias{make_table_04}
\title{FDA Table 4: Patient Disposition, Pooled Analyses}
\usage{
make_table_04(
  df,
  denominator = NULL,
  return_ard = TRUE,
  id_var = "USUBJID",
  arm_var = "TRT01A",
  saffl_var = "SAFFL",
  pop_vars = c("SAFFL"),
  lbl_pop_vars = c("Safety population")
)
}
\arguments{
\item{df}{(\code{data.frame})\cr dataset (typically ADSL) required to build the table.}

\item{denominator}{(\code{data.frame} or \code{NULL})\cr dataset used as the denominator. If \code{NULL},
\code{df} is used.}

\item{return_ard}{(\code{flag})\cr whether an ARD should be returned. Defaults to \code{TRUE}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable used to split table into columns.
Defaults to \code{"TRT01A"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{pop_vars}{(\code{character})\cr population flag variables to include in the table.
Defaults to \code{c("SAFFL")}.}

\item{lbl_pop_vars}{(\code{character})\cr labels for the population variables. Defaults to
\code{c("Safety population")}.}
}
\value{
A \code{gtsummary} table, or if \code{return_ard = TRUE}, a named list with elements \code{table} and \code{ard}.
}
\description{
Creates FDA Table 4 showing patient disposition including population counts, study drug
discontinuation reasons, and study discontinuation reasons.
}
\details{
\itemize{
\item \code{df} must contain \code{EOTSTT}, \code{DCSREAS}, \code{EOSSTT}, and the variables specified by \code{arm_var},
\code{id_var}, \code{saffl_var}, and \code{pop_vars}.
\item Flag variables (i.e. \code{XXXFL}) are expected to have two levels: \code{"Y"} (true) and \code{"N"} (false).
Missing values in flag variables are treated as \code{"N"}.
\item Discontinuation reasons are derived from \code{EOTSTT} and \code{DCSREAS} combinations.
\item Numbers in table represent the absolute numbers of patients and fraction of \code{N}.
\item When \code{return_ard = TRUE}, returns a named list with elements \code{table} and \code{ard}.
}
}
\examples{
\dontshow{if (requireNamespace("pharmaverseadam", quietly = TRUE) && requireNamespace("dplyr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

adsl <- pharmaverseadam::adsl |>
  filter(TRT01A != "Screen Failure")

result <- make_table_04(df = adsl)
result$table
\dontshow{\}) # examplesIf}
}
