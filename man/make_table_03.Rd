% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_03.R
\name{make_table_03}
\alias{make_table_03}
\title{FDA Table 3: Patient Screening and Enrollment, Trials A and B}
\usage{
make_table_03(
  df,
  denominator = NULL,
  return_ard = TRUE,
  id_var = "USUBJID",
  arm_var = "TRT01A",
  scrnfl_var = "SCRNFL",
  scrnfailfl_var = "SCRNFAILFL",
  scrnfail_var = "SCRNFRS"
)
}
\arguments{
\item{df}{(\code{data.frame})\cr dataset (typically ADSL) required to build the table.}

\item{denominator}{(\code{data.frame} or \code{NULL})\cr dataset used as the denominator. If \code{NULL},
\code{df} is used.}

\item{return_ard}{(\code{flag})\cr whether an ARD should be returned. Defaults to \code{TRUE}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable used to split table into columns.
Defaults to \code{"TRT01A"}.}

\item{scrnfl_var}{(\code{character})\cr name of the screen flag variable. Defaults to \code{"SCRNFL"}.}

\item{scrnfailfl_var}{(\code{character})\cr name of the screening failure flag variable.
Defaults to \code{"SCRNFAILFL"}.}

\item{scrnfail_var}{(\code{character})\cr name of the screening failure reason variable.
Defaults to \code{"SCRNFRS"}.}
}
\value{
A \code{gtsummary} table, or if \code{return_ard = TRUE}, a named list with elements \code{table} and \code{ard}.
}
\description{
Creates FDA Table 3 showing patient screening and enrollment, including patients screened,
screening failures and reasons, patients enrolled, and patients randomized.
}
\details{
\itemize{
\item \code{df} must contain \code{ENRLDT}, \code{RANDDT}, and the variables specified by \code{id_var}, \code{arm_var},
\code{scrnfl_var}, \code{scrnfailfl_var}, and \code{scrnfail_var}.
\item Patients are considered enrolled if they have an enrollment date (\code{ENRLDT} is not missing),
and are considered randomized if they have a randomization date (\code{RANDDT} is not missing).
\item Flag variables (i.e. \code{XXXFL}) are expected to have two levels: \code{"Y"} (true) and \code{"N"} (false).
Missing values in flag variables are treated as \code{"N"}.
\item Numbers in table represent the absolute numbers of patients and fraction of \code{N}.
\item When \code{return_ard = TRUE}, returns a named list with elements \code{table} and \code{ard}.
}
}
\examples{
\dontshow{if (requireNamespace("random.cdisc.data", quietly = TRUE) && requireNamespace("dplyr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

adsl <- random.cdisc.data::cadsl

set.seed(1)
adsl$RANDDT[sample(seq_len(nrow(adsl)), 100)] <- NA
scrnfail_reas_lvls <- c(
  "Inclusion/exclusion criteria not met", "Patient noncompliance",
  "Consent withdrawn", "Other"
)
adsl <- adsl |>
  mutate(
    ENRLDT = RANDDT,
    SCRNFL = "Y",
    SCRNFRS = factor(
      sample(scrnfail_reas_lvls, size = nrow(adsl), replace = TRUE),
      levels = scrnfail_reas_lvls
    ),
    SCRNFAILFL = ifelse(is.na(ENRLDT), "Y", "N")
  )
adsl$SCRNFRS[!is.na(adsl$ENRLDT)] <- NA

result <- make_table_03(df = adsl, denominator = adsl, arm_var = "ARM")
result$table
\dontshow{\}) # examplesIf}
}
