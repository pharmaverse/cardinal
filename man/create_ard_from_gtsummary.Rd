% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_04.R
\name{create_ard_from_gtsummary}
\alias{create_ard_from_gtsummary}
\title{Convert a \code{gtsummary::tbl_summary} Object into an ARD (Analysis Results Display) Format}
\usage{
create_ard_from_gtsummary(tbl_summary_obj)
}
\arguments{
\item{tbl_summary_obj}{A \code{gtsummary::tbl_summary} object containing summary statistics generated
from categorical or dichotomous variables.}
}
\value{
A data frame in ARD format containing the following columns:
\itemize{
\item \code{analysis_var}: name of the analysis variable,
\item \code{analysis_var_label}: label of the analysis variable,
\item \code{group_level}: level or category within the variable (e.g., "Male", "Female"), or \code{NA} for headers,
\item \code{treatment_group}: name of the treatment group,
\item \code{statistic_type}: type of statistic (e.g., \code{"count_pct"}),
\item \code{n}: extracted count (numeric),
\item \code{pct}: extracted percentage (numeric),
\item \code{formatted_result}: original formatted string from gtsummary (e.g., \code{"12 (34.3\%)"}).
}
}
\description{
This function extracts and reshapes the underlying data from a \code{gtsummary::tbl_summary} object
into a standardized flat ARD table format, including parsed statistics and structured columns.
}
\details{
The function:
\itemize{
\item Extracts relevant columns from the \code{table_body} component of the \code{tbl_summary} object,
\item Reshapes the summary statistics to a long format,
\item Parses and cleans treatment group names,
\item Parses count and percentage values from formatted result strings,
\item Adds standard ARD columns for downstream reporting.
}

This is useful when integrating summary tables into a broader ARD or CSR (Clinical Study Report) workflow.
}
\examples{
# Assuming `tbl` is a gtsummary::tbl_summary object:
# ard_df <- create_ard_from_gtsummary(tbl)

}
