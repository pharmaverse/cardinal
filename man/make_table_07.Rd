% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_07.R
\name{make_table_07}
\alias{make_table_07}
\title{FDA Table 7: Deaths, Safety Population, Pooled Analyses}
\usage{
make_table_07(
  adae,
  denominator = NULL,
  return_ard = TRUE,
  id_var = "USUBJID",
  arm_var = "TRT01A",
  saffl_var = "SAFFL"
)
}
\arguments{
\item{adae}{(\code{data.frame})\cr dataset (typically ADAE) required to build the table.}

\item{denominator}{(\code{data.frame} or \code{NULL})\cr dataset used as the denominator (typically ADSL).
If \code{NULL}, \code{adae} is used.}

\item{return_ard}{(\code{flag})\cr whether an ARD should be returned. Defaults to \code{TRUE}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable used to split table into columns.
Defaults to \code{"TRT01A"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}
}
\value{
A \code{gtsummary} table, or if \code{return_ard = TRUE}, a named list with elements \code{table} and \code{ard}.
}
\description{
Creates FDA Table 7 showing deaths split by treatment-emergent and non-treatment-emergent,
with cause of death breakdown for the safety population.
}
\details{
\itemize{
\item \code{adae} must contain \code{TRTEMFL}, \code{DTHFL}, \code{DTHCAUS}, and the variables specified by \code{arm_var},
\code{id_var}, and \code{saffl_var}.
\item If specified, \code{denominator} must contain the variables specified by \code{arm_var}, \code{id_var},
and \code{saffl_var}.
\item Flag variables (i.e. \code{XXXFL}) are expected to have two levels: \code{"Y"} (true) and \code{"N"} (false).
Missing values in flag variables are treated as \code{"N"}.
\item Rows are filtered to safety population and patients with \code{DTHFL == "Y"}.
\item Numbers in table represent the absolute numbers of patients and fraction of \code{N} for category
summary rows and fraction of \code{n} for all other rows.
\item When \code{return_ard = TRUE}, returns a named list with elements \code{table} and \code{ard}.
}
}
\examples{
\dontshow{if (requireNamespace("pharmaverseadam", quietly = TRUE) && requireNamespace("dplyr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

adsl <- pharmaverseadam::adsl |>
  filter(TRT01A != "Screen Failure")

adae <- pharmaverseadam::adae |>
  filter(DTHFL == "Y")

result <- make_table_07(adae = adae, denominator = adsl)
result$table
\dontshow{\}) # examplesIf}
}
