% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_16.R
\name{make_table_16}
\alias{make_table_16}
\title{FDA Table 16: Patients With Adverse Events by Male-Specific FDA Medical Query (Broad)
and Preferred Term, Male Safety Population, Pooled Analyses}
\usage{
make_table_16(
  df,
  denominator = NULL,
  return_ard = TRUE,
  id_var = "USUBJID",
  arm_var = "TRT01A",
  saffl_var = "SAFFL",
  pref_var = "AEDECOD",
  sex_scope = "M",
  fmq_scope = "BROAD",
  fmqsc_var = "FMQ01SC",
  fmqnam_var = "FMQ01NAM",
  na_level = "<Missing>"
)
}
\arguments{
\item{df}{(\code{data.frame})\cr dataset (typically ADAE) required to build the table.}

\item{denominator}{(\code{data.frame} or \code{NULL})\cr dataset used as the denominator (typically ADSL).
If \code{NULL}, \code{df} is used.}

\item{return_ard}{(\code{flag})\cr whether an ARD should be returned. Defaults to \code{TRUE}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable used to split table into columns.
Defaults to \code{"TRT01A"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{pref_var}{(\code{character})\cr name of the preferred term variable. Defaults to \code{"AEDECOD"}.}

\item{sex_scope}{(\code{character})\cr sex group to include. Defaults to \code{"M"} (male).}

\item{fmq_scope}{(\code{character})\cr FMQ scope: \code{"NARROW"} or \code{"BROAD"}. Defaults to \code{"BROAD"}.}

\item{fmqsc_var}{(\code{character})\cr name of the FMQ scope variable. Defaults to \code{"FMQ01SC"}.}

\item{fmqnam_var}{(\code{character})\cr name of the FMQ name variable. Defaults to \code{"FMQ01NAM"}.}

\item{na_level}{(\code{character})\cr string used to label missing values. Defaults to \code{"<Missing>"}.}
}
\value{
A \code{gtsummary} table, or if \code{return_ard = TRUE}, a named list with elements \code{table} and \code{ard}.
}
\description{
Creates FDA Table 16 showing patients with adverse events by male-specific FDA Medical Query
(Broad) and preferred term, restricted to the male safety population.
}
\details{
\itemize{
\item \code{df} must contain \code{SEX}, and the variables specified by \code{arm_var}, \code{id_var}, \code{saffl_var},
\code{pref_var}, \code{fmqsc_var}, and \code{fmqnam_var}.
\item FMQ names that appear in the non-male population are excluded from the male analysis.
\item Flag variables (i.e. \code{XXXFL}) are expected to have two levels: \code{"Y"} (true) and \code{"N"} (false).
Missing values in flag variables are treated as \code{"N"}.
\item Numbers in table represent the absolute numbers of patients and fraction of \code{N}.
\item When \code{return_ard = TRUE}, returns a named list with elements \code{table} and \code{ard}.
}
}
\examples{
\dontshow{if (requireNamespace("random.cdisc.data", quietly = TRUE) && requireNamespace("dplyr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

adsl <- random.cdisc.data::cadsl
adae <- random.cdisc.data::cadae

set.seed(1)
adae <- dplyr::rename(adae, FMQ01SC = SMQ01SC, FMQ01NAM = SMQ01NAM)
levels(adae$FMQ01SC) <- c("BROAD", "NARROW")
adae$FMQ01SC[is.na(adae$FMQ01SC)] <- "NARROW"
adae$FMQ01NAM <- factor(
  adae$FMQ01NAM,
  levels = c(unique(adae$FMQ01NAM), "Erectile Dysfunction", "Gynecomastia")
)
adae$FMQ01NAM[adae$SEX == "M"] <- as.factor(
  sample(c("Erectile Dysfunction", "Gynecomastia"), sum(adae$SEX == "M"), replace = TRUE)
)

result <- make_table_16(df = adae, denominator = adsl, arm_var = "ARM")
result$table
\dontshow{\}) # examplesIf}
}
