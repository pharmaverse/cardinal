% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-fig_02.R
\name{make_fig_02}
\alias{make_fig_02}
\title{FDA Figure 2: Time to Last Follow Up, Safety Population, Pooled Analyses}
\usage{
make_fig_02(
  df,
  arm_var = "TRT01A",
  id_var = "USUBJID",
  saffl_var = "SAFFL",
  eosdy_var = "EOSDY",
  u_trtdur = "days",
  x_lab = paste0("Time from first dose (", u_trtdur, ")"),
  y_lab = "Percent of Patients (\%)",
  xticks = NA,
  ggtheme = NULL,
  add_table = TRUE,
  annotations = NULL
)
}
\arguments{
\item{df}{(\code{data.frame})\cr dataset (typically ADSL) required to build the figure.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable used to split figure into lines.
Defaults to \code{"TRT01A"}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{eosdy_var}{(\code{character})\cr name of the last recorded study day variable. Defaults to \code{"EOSDY"}.}

\item{u_trtdur}{(\code{character})\cr unit for time. One of \code{"days"}, \code{"weeks"}, \code{"months"}, or
\code{"years"}. Defaults to \code{"days"}.}

\item{x_lab}{(\code{character})\cr x-axis label. Defaults to \code{"Time from first dose (<u_trtdur>)"}.}

\item{y_lab}{(\code{character})\cr y-axis label. Defaults to \code{"Percent of Patients (\%)"}.}

\item{xticks}{(\code{numeric} or \code{NA})\cr x-axis tick mark positions. Defaults to \code{NA} (auto).}

\item{ggtheme}{(\code{ggplot2::theme} or \code{NULL})\cr ggplot2 theme to apply. Defaults to \code{NULL}.}

\item{add_table}{(\code{flag})\cr whether to add a "Number of Patients" table below the plot.
Defaults to \code{TRUE}.}

\item{annotations}{(named \code{list} of \code{character})\cr list of annotations to add to the figure.
Valid types are \code{title}, \code{subtitles}, and \code{caption}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Creates FDA Figure 2 showing time to last follow-up for the safety population, with an optional
"Number of Patients" table below the plot.
}
\details{
\itemize{
\item \code{df} must contain the variables specified by \code{arm_var}, \code{id_var}, \code{saffl_var}, and \code{eosdy_var}.
\item Flag variables (i.e. \code{XXXFL}) are expected to have two levels: \code{"Y"} (true) and \code{"N"} (false).
Missing values in flag variables are treated as \code{"N"}.
\item Records with missing \code{eosdy_var} are excluded from all calculations.
\item Values in the "Number of Patients" table are the number of patients with time-to-last-follow-up
equal to or greater than the given time.
}
}
\examples{
\dontshow{if (requireNamespace("pharmaverseadam", quietly = TRUE) && requireNamespace("dplyr", quietly = TRUE) && requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

adsl <- pharmaverseadam::adsl |>
  filter(TRT01A != "Screen Failure")

set.seed(1)
adsl$EOSDY <- sample(0:400, nrow(adsl), replace = TRUE)

fig <- make_fig_02(df = adsl)
fig
\dontshow{\}) # examplesIf}
}
