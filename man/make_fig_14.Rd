% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-fig_14.R
\name{make_fig_14}
\alias{make_fig_14}
\title{FDA Figure 14: Mean and 95\% Confidence Interval of Systolic Blood Pressure Over Time
by Treatment Arm, Safety Population, Trial X}
\usage{
make_fig_14(
  df,
  arm_var = "TRT01A",
  saffl_var = "SAFFL",
  visit_var = "AVISIT",
  paramcd_val = "SYSBP",
  add_cond = NULL,
  x_lab = "",
  y_lab = NULL,
  yticks = NA,
  ggtheme = NULL,
  add_table = TRUE,
  annotations = NULL
)
}
\arguments{
\item{df}{(\code{data.frame})\cr vital signs dataset (typically ADVS) required to build the figure.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable used to split figure into lines.
Defaults to \code{"TRT01A"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{visit_var}{(\code{character})\cr visit variable to put on the x-axis. Defaults to \code{"AVISIT"}.}

\item{paramcd_val}{(\code{character})\cr value of \code{PARAMCD} to filter on. Defaults to \code{"SYSBP"}.}

\item{add_cond}{(\code{character} or \code{NULL})\cr character string containing an R expression to
apply as an additional filter. Defaults to \code{NULL}.}

\item{x_lab}{(\code{character})\cr x-axis label. Defaults to \code{""}.}

\item{y_lab}{(\code{character} or \code{NULL})\cr y-axis label. If \code{NULL}, derived from \code{PARAM} and \code{AVALU}.}

\item{yticks}{(\code{numeric} or \code{NA})\cr y-axis tick mark positions. Defaults to \code{NA} (auto).}

\item{ggtheme}{(\code{ggplot2::theme} or \code{NULL})\cr ggplot2 theme to apply. Defaults to \code{NULL}.}

\item{add_table}{(\code{flag})\cr whether to add "Mean Value" and "Number of Patients" tables below
the plot. Defaults to \code{TRUE}.}

\item{annotations}{(named \code{list} of \code{character})\cr list of annotations to add to the figure.
Valid types are \code{title}, \code{subtitles}, and \code{caption}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Creates FDA Figure 14 showing mean systolic blood pressure with 95\% confidence intervals
over time by treatment arm, with optional "Mean Value" and "Number of Patients" tables
below the plot.
}
\details{
\itemize{
\item \code{df} must contain \code{AVAL}, \code{PARAMCD}, \code{PARAM}, \code{AVALU}, and the variables specified by \code{arm_var},
\code{saffl_var}, and \code{visit_var}.
\item Flag variables (i.e. \code{XXXFL}) are expected to have two levels: \code{"Y"} (true) and \code{"N"} (false).
Missing values in flag variables are treated as \code{"N"}.
\item Mean and 95\% CI are computed per arm and visit using t-distribution critical values.
}
}
\examples{
\dontshow{if (requireNamespace("random.cdisc.data", quietly = TRUE) && requireNamespace("ggplot2", quietly = TRUE) && requireNamespace("dplyr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

advs <- random.cdisc.data::cadvs

fig <- make_fig_14(
  df = advs,
  arm_var = "ARM",
  add_cond = "ONTRTFL == 'Y' | ABLFL == 'Y'",
  add_table = TRUE,
  yticks = c(135, 140, 145, 150, 155, 160)
)
fig
\dontshow{\}) # examplesIf}
}
