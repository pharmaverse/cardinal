% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_08.R
\name{make_table_08}
\alias{make_table_08}
\title{FDA Table 8: All Individual Patient Deaths, Safety Population, Pooled Analyses}
\usage{
make_table_08(
  adae,
  adex,
  arm_var = "TRT01A",
  id_var = "USUBJID",
  saffl_var = "SAFFL",
  dth_vars = c("DTHCAUS", "DTHCAT"),
  lbl_dth_vars = c("Cause of Death\\nMedDRA\\nPreferred Term",
    "Cause of Death\\nVerbatim Term"),
  na_level = "NA"
)
}
\arguments{
\item{adae}{(\code{data.frame})\cr adverse events dataset (typically ADAE) containing death records.}

\item{adex}{(\code{data.frame})\cr exposure dataset (typically ADEX) containing dosing information.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable. Defaults to \code{"TRT01A"}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{dth_vars}{(\code{character})\cr additional death variables from \code{adae} to include in the listing.
Defaults to \code{c("DTHCAUS", "DTHCAT")}.}

\item{lbl_dth_vars}{(\code{character})\cr labels for the variables in \code{dth_vars}. Defaults to cause of
death labels.}

\item{na_level}{(\code{character})\cr string used to represent missing values. Defaults to \code{"NA"}.}
}
\value{
A \code{listing_df} object.
}
\description{
Creates FDA Table 8 as a patient-level listing of all individual deaths in the safety population,
including demographics, dosage, study day of death, and cause of death.
}
\details{
\itemize{
\item \code{adae} must contain \code{AGE}, \code{SEX}, \code{AESDTH}, \code{DTHADY}, the variables specified by \code{dth_vars},
\code{arm_var}, \code{id_var}, and \code{saffl_var}.
\item \code{adex} must contain \code{PARAMCD}, \code{TRTSDT}, \code{TRTEDT}, \code{AVAL}, \code{AVALU}, and the variables specified
by \code{arm_var}, \code{id_var}, and \code{saffl_var}.
\item Flag variables (i.e. \code{XXXFL}) are expected to have two levels: \code{"Y"} (true) and \code{"N"} (false).
Missing values in flag variables are treated as \code{"N"}.
\item Returns a \code{listing_df} object from the \code{rlistings} package.
}
}
\examples{
\dontshow{if (requireNamespace("random.cdisc.data", quietly = TRUE) && requireNamespace("rlistings", quietly = TRUE) && requireNamespace("dplyr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
adae <- random.cdisc.data::cadae
adex <- random.cdisc.data::cadex

result <- make_table_08(adae = adae, adex = adex, arm_var = "ARM")
head(result, 20)
\dontshow{\}) # examplesIf}
}
