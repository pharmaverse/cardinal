% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_51.R
\name{make_table_51}
\alias{make_table_51}
\title{FDA Table 51: Adverse Events by Demographic Subgroup,
Safety Population, Pooled Analysis}
\usage{
make_table_51(
  df,
  denominator,
  return_ard = TRUE,
  id_var = "USUBJID",
  arm_var = "ARM",
  sex_var = "SEX",
  age_var = "AGEGR1",
  race_var = "RACE",
  ethnic_var = "ETHNIC"
)
}
\arguments{
\item{df}{(\code{data.frame})\cr dataset (typically ADAE) required to build the table. Should be
pre-filtered to one row per subject (e.g. using \code{dplyr::slice_tail(by=c(id_var, arm_var))}).}

\item{denominator}{(\code{data.frame})\cr safety-filtered ADSL dataset used as the denominator.}

\item{return_ard}{(\code{flag})\cr whether an ARD should be returned. Defaults to \code{TRUE}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable. Defaults to \code{"ARM"}.}

\item{sex_var}{(\code{character})\cr name of the sex variable. Defaults to \code{"SEX"}.}

\item{age_var}{(\code{character})\cr name of the age group variable. Defaults to \code{"AGEGR1"}.}

\item{race_var}{(\code{character})\cr name of the race variable. Defaults to \code{"RACE"}.}

\item{ethnic_var}{(\code{character})\cr name of the ethnicity variable. Defaults to \code{"ETHNIC"}.}
}
\value{
A \code{gtsummary} table, or if \code{return_ard = TRUE}, a named list with elements \code{table} and \code{ard}.
}
\description{
Creates FDA Table 51 showing any treatment-emergent adverse event and AE counts stratified
by demographic subgroups (sex, age group, race, ethnicity) using a custom ARD approach.
}
\details{
\itemize{
\item \code{df} should be pre-filtered to treatment-emergent AEs (\code{TRTEMFL == "Y"}) and one AE per
subject (e.g. \code{dplyr::slice_tail(by = c(id_var, arm_var))}) before calling this function.
A derived \code{any_ae} column is added internally.
\item \code{denominator} must be pre-filtered to the safety population and contain \code{arm_var} and the
demographic variables.
\item Numbers in table represent the absolute numbers of patients and fraction of the safety N.
\item When \code{return_ard = TRUE}, returns a named list with elements \code{table} and \code{ard}.
}
}
\examples{
\dontshow{if (requireNamespace("random.cdisc.data", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

adsl <- random.cdisc.data::cadsl |>
  filter(SAFFL == "Y") |>
  mutate(any_ae = "Any AE")
adae <- random.cdisc.data::cadae |>
  filter(SAFFL == "Y", TRTEMFL == "Y") |>
  slice_tail(n = 1L, by = all_of(c("USUBJID", "ARM")))

result <- make_table_51(df = adae, denominator = adsl, arm_var = "ARM")
result$table
\dontshow{\}) # examplesIf}
}
