% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_37.R
\name{make_table_37}
\alias{make_table_37}
\title{FDA Table 37: Percentage of Patients Meeting Specific Diastolic Blood Pressure Levels
Postbaseline, Safety Population, Pooled Analysis}
\usage{
make_table_37(
  df,
  denominator,
  return_ard = TRUE,
  id_var = "USUBJID",
  arm_var = "TRT01A",
  saffl_var = "SAFFL",
  paramcd = "DIABP",
  avisitn_min = 2
)
}
\arguments{
\item{df}{(\code{data.frame})\cr vital signs dataset (typically ADVS) required to build the table.}

\item{denominator}{(\code{data.frame})\cr dataset used as the denominator for header N values.
Typically ADSL pre-filtered to the safety population. Must contain \code{arm_var}.}

\item{return_ard}{(\code{flag})\cr whether an ARD should be returned. Defaults to \code{TRUE}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable used to split table into columns.
Defaults to \code{"TRT01A"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{paramcd}{(\code{character})\cr value of \code{PARAMCD} to filter on. Defaults to \code{"DIABP"}.}

\item{avisitn_min}{(\code{numeric})\cr minimum value of \code{AVISITN} for the postbaseline filter.
Defaults to \code{2}.}
}
\value{
A \code{gtsummary} table, or if \code{return_ard = TRUE}, a named list with elements \code{table} and \code{ard}.
}
\description{
Creates FDA Table 37 showing the percentage of patients meeting specific diastolic blood pressure
cutoff criteria based on the maximum postbaseline value per subject.
}
\details{
\itemize{
\item \code{df} must contain \code{PARAMCD}, \code{AVAL}, \code{AVISITN}, \code{VSORRESU}, and the variables specified by
\code{saffl_var}, \code{arm_var}, and \code{id_var}.
\item Rows are filtered to safety population (\code{saffl_var == "Y"}), \code{PARAMCD == paramcd}, and
\code{AVISITN >= avisitn_min}. The maximum \code{AVAL} per subject is selected.
\item Five cutoff flags are derived: \code{L60} (\code{AVAL < 60}), \code{G60} (\code{AVAL > 60}),
\code{G90} (\code{AVAL > 90}), \code{G110} (\code{AVAL > 110}), \code{GE120} (\code{AVAL >= 120}).
\item \code{denominator} should already be pre-filtered to the safety population.
\item Numbers in table represent the absolute numbers of patients and fraction of \code{N}.
\item When \code{return_ard = TRUE}, returns a named list with elements \code{table} and \code{ard}.
}
}
\examples{
\dontshow{if (requireNamespace("pharmaverseadam", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

adsl <- pharmaverseadam::adsl |> filter(SAFFL == "Y")
advs <- pharmaverseadam::advs

result <- make_table_37(df = advs, denominator = adsl)
result$table
\dontshow{\}) # examplesIf}
}
