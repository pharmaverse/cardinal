% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_50.R
\name{make_table_50}
\alias{make_table_50}
\title{FDA Table 50: Serious Adverse Events by Demographic Subgroup,
Safety Population, Pooled Analysis}
\usage{
make_table_50(
  df,
  denominator,
  return_ard = TRUE,
  id_var = "USUBJID",
  arm_var = "TRT01A",
  saffl_var = "SAFFL",
  trtemfl_var = "TRTEMFL",
  ser_var = "AESER",
  sex_var = "SEX",
  age_var = "AGEGR1",
  race_var = "RACE",
  ethnic_var = "ETHNIC"
)
}
\arguments{
\item{df}{(\code{data.frame})\cr dataset (typically ADAE) required to build the table.}

\item{denominator}{(\code{data.frame})\cr safety-filtered ADSL dataset used as the denominator.}

\item{return_ard}{(\code{flag})\cr whether an ARD should be returned. Defaults to \code{TRUE}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable. Defaults to \code{"TRT01A"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{trtemfl_var}{(\code{character})\cr name of the treatment-emergent flag variable.
Defaults to \code{"TRTEMFL"}.}

\item{ser_var}{(\code{character})\cr name of the variable indicating serious AEs. Defaults to \code{"AESER"}.}

\item{sex_var}{(\code{character})\cr name of the sex variable. Defaults to \code{"SEX"}.}

\item{age_var}{(\code{character})\cr name of the age group variable. Defaults to \code{"AGEGR1"}.}

\item{race_var}{(\code{character})\cr name of the race variable. Defaults to \code{"RACE"}.}

\item{ethnic_var}{(\code{character})\cr name of the ethnicity variable. Defaults to \code{"ETHNIC"}.}
}
\value{
A \code{gtsummary} table, or if \code{return_ard = TRUE}, a named list with elements \code{table} and \code{ard}.
}
\description{
Creates FDA Table 50 showing any SAE and SAE counts stratified by demographic subgroups
(sex, age group, race, ethnicity) for the safety population.
}
\details{
\itemize{
\item \code{df} must contain \code{saffl_var}, \code{trtemfl_var}, \code{ser_var}, \code{arm_var}, \code{id_var},
\code{sex_var}, \code{age_var}, \code{race_var}, and \code{ethnic_var}.
\item \code{denominator} must be pre-filtered to the safety population and contain \code{arm_var} and \code{id_var}.
\item The table stacks: any SAE row + sex breakdown + age group breakdown +
race breakdown + ethnicity breakdown.
\item Numbers in table represent the absolute numbers of patients and fraction of \code{N}.
\item When \code{return_ard = TRUE}, returns a named list with elements \code{table} and \code{ard}.
}
}
\examples{
\dontshow{if (requireNamespace("pharmaverseadam", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

adsl <- pharmaverseadam::adsl |>
  filter(SAFFL == "Y") |>
  mutate(TRT01A = as.factor(TRT01A))
adae <- pharmaverseadam::adae |>
  mutate(TRT01A = as.factor(TRT01A))

result <- make_table_50(df = adae, denominator = adsl)
result$table
\dontshow{\}) # examplesIf}
}
