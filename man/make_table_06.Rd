% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_06.R
\name{make_table_06}
\alias{make_table_06}
\title{FDA Table 6: Overview of Adverse Events, Safety Population, Pooled Analyses}
\usage{
make_table_06(
  df,
  denominator,
  return_ard = TRUE,
  id_var = "USUBJID",
  arm_var = "TRT01A",
  saffl_var = "SAFFL",
  sae_var = "AESER",
  sae_cat_vars = list(`SAEs with fatal outcome` = "AESDTH", `Life-threatening SAEs` =
    "AESLIFE", `SAEs requiring hospitalization` = "AESHOSP",
    `SAEs resulting in substantial disruption of normal life functions` = "AESDISAB",
    `Congenital anomaly or birth defect` = "AESCONG", Other = "AESMIE"),
  disc_var = "AEACNOTH",
  dose_mod_var = "AEACN",
  dose_mod_cat_labels = list(`DRUG INTERRUPTED` =
    "AE leading to interruption of study drug", `DOSE REDUCED` =
    "AE leading to reduction of study drug", `DOSE RATE REDUCED` =
    "AE leading to dose delay of study drug", `DOSE INCREASED` = "Other"),
  sev_var = "AESEV"
)
}
\arguments{
\item{df}{(\code{data.frame})\cr dataset (typically ADAE) required to build the table.}

\item{denominator}{(\code{data.frame})\cr dataset used as the denominator for proportion calculations.
Typically ADSL. Cannot be missing.}

\item{return_ard}{(\code{flag})\cr whether an ARD should be returned. Defaults to \code{TRUE}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable used to split table into columns.
Defaults to \code{"TRT01A"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{sae_var}{(\code{character})\cr name of the flag variable indicating serious AEs. Defaults to \code{"AESER"}.}

\item{sae_cat_vars}{(named \code{list} of \code{character})\cr list of flag variable names for categories of
serious AEs. Defaults to \code{AESDTH}, \code{AESLIFE}, \code{AESHOSP}, \code{AESDISAB}, \code{AESCONG}, \code{AESMIE}.}

\item{disc_var}{(\code{character})\cr name of the variable for study discontinuation due to AE.
Defaults to \code{"AEACNOTH"}.}

\item{dose_mod_var}{(\code{character})\cr name of the variable for dose modification due to AE.
Defaults to \code{"AEACN"}.}

\item{dose_mod_cat_labels}{(named \code{list} of \code{character})\cr list of labels for dose modification
categories.}

\item{sev_var}{(\code{character})\cr name of the AE severity variable. Defaults to \code{"AESEV"}.}
}
\value{
A \code{gtsummary} table, or if \code{return_ard = TRUE}, a named list with elements \code{table} and \code{ard}.
}
\description{
Creates FDA Table 6 showing an overview of adverse events including serious AEs,
discontinuations, dose modifications, and overall AE counts.
}
\details{
Typically, an ADAE ADaM dataset is passed to \code{df} and an ADSL dataset to \code{denominator}. \cr
The values passed through \code{id_var} and \code{arm_var} must be present in \code{names(denominator)}. \cr
The values passed through \code{saffl_var}, \code{arm_var}, \code{sae_var}, \code{sae_cat_vars}, \code{disc_var},
\code{dose_mod_var}, and \code{sev_var} must be present in \code{names(df)}. \cr
Discontinuation is indicated by \code{"SUBJECT DISCONTINUED FROM STUDY"} in \code{disc_var}. \cr
Dose modification may be \code{"DRUG INTERRUPTED"}, \code{"DOSE REDUCED"}, \code{"DOSE RATE REDUCED"},
\code{"DOSE INCREASED"}.
}
\examples{
\dontshow{if (requireNamespace("pharmaverseadam", quietly = TRUE) && requireNamespace("dplyr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

adsl <- pharmaverseadam::adsl |>
  filter(TRT01A != "Screen Failure")

adae <- pharmaverseadam::adae

result <- make_table_06(df = adae, denominator = adsl)
result$table
\dontshow{\}) # examplesIf}
}
