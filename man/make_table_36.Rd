% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_36.R
\name{make_table_36}
\alias{make_table_36}
\title{FDA Table 36: Patients With Adverse Events by System Organ Class and Preferred Term,
Safety Population, Pooled Analysis (or Trial X)}
\usage{
make_table_36(
  df,
  denominator = NULL,
  return_ard = TRUE,
  id_var = "USUBJID",
  arm_var = "TRT01A",
  saffl_var = "SAFFL",
  soc_var = "AESOC",
  pref_var = "AEDECOD"
)
}
\arguments{
\item{df}{(\code{data.frame})\cr dataset (typically ADAE) required to build the table.}

\item{denominator}{(\code{data.frame} or \code{NULL})\cr dataset used as the denominator (typically ADSL).
If \code{NULL}, \code{df} is used.}

\item{return_ard}{(\code{flag})\cr whether an ARD should be returned. Defaults to \code{TRUE}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable used to split table into columns.
Defaults to \code{"TRT01A"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{soc_var}{(\code{character})\cr name of the system organ class variable. Defaults to \code{"AESOC"}.}

\item{pref_var}{(\code{character})\cr name of the preferred term variable. Defaults to \code{"AEDECOD"}.}
}
\value{
A \code{gtsummary} table, or if \code{return_ard = TRUE}, a named list with elements \code{table} and \code{ard}.
}
\description{
Creates FDA Table 36 showing patients with adverse events by system organ class and
preferred term for the safety population.
}
\details{
\itemize{
\item \code{df} must contain the variables specified by \code{id_var}, \code{arm_var}, \code{saffl_var}, \code{soc_var},
and \code{pref_var}.
\item If specified, \code{denominator} must contain the variables specified by \code{arm_var}, \code{id_var},
and \code{saffl_var}.
\item Rows are filtered to the safety population (\code{saffl_var == "Y"}).
\item Numbers in table represent the absolute numbers of patients and fraction of \code{N}.
\item When \code{return_ard = TRUE}, returns a named list with elements \code{table} and \code{ard}.
}
}
\examples{
\dontshow{if (requireNamespace("pharmaverseadam", quietly = TRUE) && requireNamespace("dplyr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

adsl <- pharmaverseadam::adsl |>
  filter(TRT01A != "Screen Failure")

adae <- pharmaverseadam::adae

result <- make_table_36(df = adae, denominator = adsl)
result$table
\dontshow{\}) # examplesIf}
}
