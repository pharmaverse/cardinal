% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_22.R
\name{make_table_22}
\alias{make_table_22}
\title{FDA Table 22: Overview of Adverse Events by Demographic Subgroup, Safety Population,
Pooled Analysis}
\usage{
make_table_22(
  df,
  denominator = NULL,
  return_ard = TRUE,
  id_var = "USUBJID",
  arm_var = "TRT01A",
  saffl_var = "SAFFL",
  trtemfl_var = "TRTEMFL",
  vars = c("SEX", "AGEGR1", "RACE", "ETHNIC"),
  lbl_overall = NULL
)
}
\arguments{
\item{df}{(\code{data.frame})\cr dataset (typically ADAE joined with ADSL) required to build the table.}

\item{denominator}{(\code{data.frame} or \code{NULL})\cr dataset used as the denominator (typically ADSL).
If \code{NULL}, \code{df} is used.}

\item{return_ard}{(\code{flag})\cr whether an ARD should be returned. Defaults to \code{TRUE}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable used to split table into columns.
Defaults to \code{"TRT01A"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{trtemfl_var}{(\code{character})\cr name of the treatment-emergent flag variable.
Defaults to \code{"TRTEMFL"}.}

\item{vars}{(\code{character})\cr demographic subgroup variables to include in the table.
Defaults to \code{c("SEX", "AGEGR1", "RACE", "ETHNIC")}.}

\item{lbl_overall}{(\code{character} or \code{NULL})\cr label for the overall column. If \code{NULL} (default),
no overall column is added.}
}
\value{
A \code{gtsummary} table, or if \code{return_ard = TRUE}, a named list with elements \code{table} and \code{ard}.
}
\description{
Creates FDA Table 22 showing an overview of adverse events by demographic subgroup,
including any treatment-emergent AE and counts by subgroup categories.
}
\details{
\itemize{
\item \code{df} must contain the variables specified by \code{vars}, \code{arm_var}, \code{id_var}, \code{saffl_var},
and \code{trtemfl_var}.
\item Information from ADSL is typically joined into \code{df} prior to calling this function to provide
demographic variables.
\item Flag variables (i.e. \code{XXXFL}) are expected to have two levels: \code{"Y"} (true) and \code{"N"} (false).
Missing values in flag variables are treated as \code{"N"}.
\item Numbers represent the absolute number of patients with an AE and fraction of \code{N}.
\item When \code{return_ard = TRUE}, returns a named list with elements \code{table} and \code{ard}.
}
}
\examples{
\dontshow{if (requireNamespace("random.cdisc.data", quietly = TRUE) && requireNamespace("dplyr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

adsl <- random.cdisc.data::cadsl |>
  mutate(AGEGR1 = as.factor(case_when(
    AGE >= 17 & AGE < 65 ~ ">=17 to <65",
    AGE >= 65 ~ ">=65"
  )))

adae <- random.cdisc.data::cadae

df <- left_join(adsl, adae, by = intersect(names(adsl), names(adae)))

result <- make_table_22(df = df, denominator = adsl, arm_var = "ARM")
result$table
\dontshow{\}) # examplesIf}
}
