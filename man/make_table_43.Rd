% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_43.R
\name{make_table_43}
\alias{make_table_43}
\title{FDA Table 43: Patients With Adverse Events by System Organ Class, FDA Medical Query (Broad)
and Preferred Term, Safety Population, Pooled Analysis}
\usage{
make_table_43(
  df,
  denominator,
  return_ard = TRUE,
  id_var = "USUBJID",
  arm_var = "TRT01A",
  saffl_var = "SAFFL",
  trtemfl_var = "TRTEMFL",
  soc_var = "AEBODSYS",
  ocmqsc_var = "OCMQ01SC",
  ocmqnam_var = "OCMQ01NAM",
  pref_var = "AEDECOD",
  ocmq_scope = "BROAD"
)
}
\arguments{
\item{df}{(\code{data.frame})\cr dataset (typically ADAE) required to build the table.}

\item{denominator}{(\code{data.frame})\cr dataset used as the denominator for proportion calculations.
Typically safety-filtered ADSL. Cannot be \code{NULL}.}

\item{return_ard}{(\code{flag})\cr whether an ARD should be returned. Defaults to \code{TRUE}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable. Defaults to \code{"TRT01A"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{trtemfl_var}{(\code{character})\cr name of the treatment-emergent flag variable.
Defaults to \code{"TRTEMFL"}.}

\item{soc_var}{(\code{character})\cr name of the system organ class (body system) variable.
Defaults to \code{"AEBODSYS"}.}

\item{ocmqsc_var}{(\code{character})\cr name of the OCMQ scope variable. Defaults to \code{"OCMQ01SC"}.}

\item{ocmqnam_var}{(\code{character})\cr name of the OCMQ name variable. Defaults to \code{"OCMQ01NAM"}.}

\item{pref_var}{(\code{character})\cr name of the preferred term variable. Defaults to \code{"AEDECOD"}.}

\item{ocmq_scope}{(\code{character})\cr OCMQ scope to filter on: \code{"NARROW"} or \code{"BROAD"}.
Defaults to \code{"BROAD"}.}
}
\value{
A \code{gtsummary} table, or if \code{return_ard = TRUE}, a named list with elements \code{table} and \code{ard}.
}
\description{
Creates FDA Table 43 showing patients with adverse events by SOC, FDA Medical Query (OCMQ,
Broad scope) and Preferred Term for the safety population.
}
\details{
\itemize{
\item \code{df} must contain the variables specified by \code{id_var}, \code{arm_var}, \code{saffl_var},
\code{trtemfl_var}, \code{soc_var}, \code{ocmqsc_var}, \code{ocmqnam_var}, and \code{pref_var}.
\item \code{denominator} must contain the variables specified by \code{id_var} and \code{arm_var}.
\item Rows are filtered to safety population (\code{saffl_var == "Y"}) and treatment-emergent AEs
(\code{trtemfl_var == "Y"}) with the specified OCMQ scope.
\item Numbers in table represent the absolute numbers of patients and fraction of \code{N}.
\item When \code{return_ard = TRUE}, returns a named list with elements \code{table} and \code{ard}.
}
}
\examples{
\dontshow{if (requireNamespace("pharmaverseadam", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(1)
adsl <- pharmaverseadam::adsl |>
  dplyr::filter(SAFFL == "Y")
adae <- pharmaverseadam::adae |>
  dplyr::mutate(
    OCMQ01NAM = sample(c("OCMQ1", "OCMQ2", "OCMQ3"), dplyr::n(), replace = TRUE),
    OCMQ01SC = sample(c("BROAD", "NARROW"), dplyr::n(), replace = TRUE)
  )

result <- make_table_43(df = adae, denominator = adsl)
result$table
\dontshow{\}) # examplesIf}
}
