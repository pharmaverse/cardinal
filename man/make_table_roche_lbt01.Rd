% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-roche_lbt01.R
\name{make_table_roche_lbt01}
\alias{make_table_roche_lbt01}
\title{Roche LBT01: Laboratory Test Results by Visit, Safety Population}
\usage{
make_table_roche_lbt01(
  df,
  denominator,
  return_ard = TRUE,
  arm_var = "TRT01A",
  saffl_var = "SAFFL",
  paramcd_filter = c("ALT", "BILI", "CREAT"),
  visits = c("Baseline", "Week 4"),
  baseline_level = "Baseline"
)
}
\arguments{
\item{df}{(\code{data.frame})\cr laboratory dataset (typically ADLB) required to build the table.}

\item{denominator}{(\code{data.frame})\cr ADSL dataset used as the denominator for proportion
calculations. Typically filtered to exclude screen failures.}

\item{return_ard}{(\code{flag})\cr whether an ARD should be returned. Defaults to \code{TRUE}.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable. Defaults to \code{"TRT01A"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{paramcd_filter}{(\code{character})\cr vector of \code{PARAMCD} values to include.
Defaults to \code{c("ALT", "BILI", "CREAT")}.}

\item{visits}{(\code{character})\cr vector of visit labels to include. Defaults to
\code{c("Baseline", "Week 4")}.}

\item{baseline_level}{(\code{character})\cr visit label used as the baseline. Defaults to
\code{"Baseline"}.}
}
\value{
A \code{gtsummary} table, or if \code{return_ard = TRUE}, a named list with elements \code{table} and \code{ard}.
}
\description{
Creates a laboratory baseline-change table (Roche LBT01) showing results and changes from
baseline by visit for selected lab parameters.
}
\details{
\itemize{
\item \code{df} must contain \code{PARAMCD}, \code{AVISIT}, \code{saffl_var}, and \code{arm_var}.
\item \code{denominator} must contain \code{arm_var} and must be pre-filtered to exclude screen failures
(the table uses \code{denominator} as-is for N calculation).
\item Requires the \code{crane} package for \code{tbl_baseline_chg}.
\item When \code{return_ard = TRUE}, returns a named list with elements \code{table} and \code{ard}.
}
}
\examples{
\dontshow{if (requireNamespace("pharmaverseadam", quietly = TRUE) && requireNamespace("crane", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

adsl <- pharmaverseadam::adsl |> filter(TRT01A != "Screen Failure")
adlb <- pharmaverseadam::adlb

result <- make_table_roche_lbt01(df = adlb, denominator = adsl)
result$table
\dontshow{\}) # examplesIf}
}
