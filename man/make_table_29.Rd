% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-table_29.R
\name{make_table_29}
\alias{make_table_29}
\title{FDA Table 29: Patients With Serious Adverse Events by System Organ Class, OCMQ (Narrow Scope)
and Preferred Term, Safety Population, Pooled Analyses}
\usage{
make_table_29(
  df,
  denominator,
  return_ard = TRUE,
  id_var = "USUBJID",
  arm_var = "TRT01A",
  saffl_var = "SAFFL",
  ser_var = "AESER",
  trtemfl_var = "TRTEMFL",
  soc_var = "AEBODSYS",
  ocmqsc_var = "OCMQ01SC",
  ocmqnam_var = "OCMQ01NAM",
  pref_var = "AEDECOD",
  ocmq_scope = "NARROW"
)
}
\arguments{
\item{df}{(\code{data.frame})\cr adverse events dataset (typically ADAE) required to build the table.}

\item{denominator}{(\code{data.frame})\cr dataset used as the denominator for proportion calculations.
Typically ADSL pre-filtered to the safety population. Must contain \code{id_var} and \code{arm_var}.}

\item{return_ard}{(\code{flag})\cr whether an ARD should be returned. Defaults to \code{TRUE}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable used to split table into columns.
Defaults to \code{"TRT01A"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{ser_var}{(\code{character})\cr name of the variable indicating serious adverse events.
Defaults to \code{"AESER"}.}

\item{trtemfl_var}{(\code{character})\cr name of the treatment-emergent AE flag variable.
Defaults to \code{"TRTEMFL"}.}

\item{soc_var}{(\code{character})\cr name of the system organ class variable. Defaults to \code{"AEBODSYS"}.}

\item{ocmqsc_var}{(\code{character})\cr name of the OCMQ scope variable. Defaults to \code{"OCMQ01SC"}.}

\item{ocmqnam_var}{(\code{character})\cr name of the OCMQ name variable. Defaults to \code{"OCMQ01NAM"}.}

\item{pref_var}{(\code{character})\cr name of the preferred term variable. Defaults to \code{"AEDECOD"}.}

\item{ocmq_scope}{(\code{character})\cr OCMQ scope to filter on: \code{"NARROW"} or \code{"BROAD"}.
Defaults to \code{"NARROW"}.}
}
\value{
A \code{gtsummary} table, or if \code{return_ard = TRUE}, a named list with elements \code{table} and \code{ard}.
}
\description{
Creates FDA Table 29 showing patients with serious adverse events by system organ class,
oncology condition medical query (OCMQ, narrow scope) and preferred term for the safety
population.
}
\details{
\itemize{
\item \code{df} must contain the variables specified by \code{id_var}, \code{arm_var}, \code{saffl_var}, \code{ser_var},
\code{trtemfl_var}, \code{soc_var}, \code{ocmqsc_var}, \code{ocmqnam_var}, and \code{pref_var}.
\item \code{denominator} must contain the variables specified by \code{id_var} and \code{arm_var} and should be
pre-filtered to the safety population before passing to this function.
\item Rows in \code{df} are filtered to safety population (\code{saffl_var == "Y"}), serious AEs
(\code{ser_var == "Y"}), treatment-emergent AEs (\code{trtemfl_var == "Y"}), and the specified OCMQ
scope (\code{ocmqsc_var == toupper(ocmq_scope)}).
\item Numbers in table represent the absolute numbers of patients and fraction of \code{N}.
\item When \code{return_ard = TRUE}, returns a named list with elements \code{table} and \code{ard}.
}
}
\examples{
\dontshow{if (requireNamespace("pharmaverseadam", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

set.seed(1)
adae <- pharmaverseadam::adae |>
  rename(OCMQ01SC = AEHLTCD) |>
  mutate(
    AESER = sample(c("Y", "N"), size = nrow(pharmaverseadam::adae), replace = TRUE),
    OCMQ01NAM = sample(c("OCMQ1", "OCMQ2", "OCMQ3"), size = nrow(pharmaverseadam::adae), replace = TRUE)
  ) |>
  filter(AESOC == "VASCULAR DISORDERS")
adae$OCMQ01SC[is.na(adae$OCMQ01SC)] <- "NARROW"

adsl <- pharmaverseadam::adsl |> filter(SAFFL == "Y")

result <- make_table_29(df = adae, denominator = adsl)
result$table
\dontshow{\}) # examplesIf}
}
