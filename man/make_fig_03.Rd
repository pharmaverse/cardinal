% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fda-fig_03.R
\name{make_fig_03}
\alias{make_fig_03}
\title{FDA Figure 3: Time to Adverse Event Leading to Treatment Discontinuation, Safety Population, Trial X}
\usage{
make_fig_03(
  df,
  arm_var = "TRT01A",
  id_var = "USUBJID",
  saffl_var = "SAFFL",
  trtsdtm_var = "TRTSDTM",
  trtedtm_var = "TRTEDTM",
  u_trtdur = "days",
  dcsreas_var = "DCSREAS",
  x_lab = paste0("Time from first dose (", u_trtdur, ")"),
  y_lab = "Cumulative Incidence (\%)\\nAEs Leading to Treatment\\nDiscontinuation",
  xticks = NA,
  ggtheme = NULL,
  add_table = TRUE,
  annotations = NULL
)
}
\arguments{
\item{df}{(\code{data.frame})\cr dataset (typically ADSL) required to build the figure.}

\item{arm_var}{(\code{character})\cr name of the treatment arm variable used to split figure into lines.
Defaults to \code{"TRT01A"}.}

\item{id_var}{(\code{character})\cr name of the subject identifier variable. Defaults to \code{"USUBJID"}.}

\item{saffl_var}{(\code{character})\cr name of the safety flag variable. Defaults to \code{"SAFFL"}.}

\item{trtsdtm_var}{(\code{character})\cr name of the treatment start datetime variable.
Defaults to \code{"TRTSDTM"}.}

\item{trtedtm_var}{(\code{character})\cr name of the treatment end datetime variable.
Defaults to \code{"TRTEDTM"}.}

\item{u_trtdur}{(\code{character})\cr unit for treatment duration. One of \code{"days"}, \code{"weeks"},
\code{"months"}, or \code{"years"}. Defaults to \code{"days"}.}

\item{dcsreas_var}{(\code{character})\cr reason for treatment discontinuation variable.
Defaults to \code{"DCSREAS"}.}

\item{x_lab}{(\code{character})\cr x-axis label. Defaults to \code{"Time from first dose (<u_trtdur>)"}.}

\item{y_lab}{(\code{character})\cr y-axis label.}

\item{xticks}{(\code{numeric} or \code{NA})\cr x-axis tick mark positions. Defaults to \code{NA} (auto).}

\item{ggtheme}{(\code{ggplot2::theme} or \code{NULL})\cr ggplot2 theme to apply. Defaults to \code{NULL}.}

\item{add_table}{(\code{flag})\cr whether to add tables below the plot. Defaults to \code{TRUE}.}

\item{annotations}{(named \code{list} of \code{character})\cr list of annotations to add to the figure.
Valid types are \code{title}, \code{subtitles}, and \code{caption}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Creates FDA Figure 3 showing cumulative incidence of adverse events leading to treatment
discontinuation, using a survival analysis approach, with optional "Number at risk" and
"Cumulative Number of Patients with Event" tables below the plot.
}
\details{
\itemize{
\item \code{df} must contain the variables specified by \code{arm_var}, \code{id_var}, \code{saffl_var}, \code{trtsdtm_var},
\code{trtedtm_var}, and \code{dcsreas_var}.
\item Flag variables (i.e. \code{XXXFL}) are expected to have two levels: \code{"Y"} (true) and \code{"N"} (false).
Missing values in flag variables are treated as \code{"N"}.
\item Records with missing treatment start and/or end datetime are excluded.
\item The event indicator is \code{dcsreas_var == "ADVERSE EVENT"}.
\item Requires the \code{ggsurvfit} package for the survival plot.
}
}
\examples{
\dontshow{if (requireNamespace("random.cdisc.data", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(dplyr)

adsl <- random.cdisc.data::cadsl

fig <- make_fig_03(df = adsl, dcsreas_var = "DCSREAS", arm_var = "ARM")
fig
\dontshow{\}) # examplesIf}
}
